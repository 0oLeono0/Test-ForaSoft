import { MAX_PARTICIPANTS } from '@vcr/shared';

// Заглушка точки входа. Bootstrap (config → http(s) → Express → Socket.io) — задача 4.3.
console.log(`video-chat-room server: stub (MAX_PARTICIPANTS=${MAX_PARTICIPANTS})`);
